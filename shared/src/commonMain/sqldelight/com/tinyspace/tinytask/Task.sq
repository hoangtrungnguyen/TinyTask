CREATE TABLE tasks(
 id INTEGER PRIMARY KEY,
 title TEXT NOT NULL,
 description TEXT
);



selectAll:
SELECT * FROM tasks;

insert:
INSERT OR REPLACE INTO tasks(title, description)
VALUES (?, ?    );

